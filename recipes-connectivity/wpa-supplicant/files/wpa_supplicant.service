[Unit]
Description=WPA supplicant
Before=network.target
After=dbus.service

[Service]
Type=simple
ExecStart=/usr/sbin/wpa_supplicant -i wlan0 -c /etc/wpa_supplicant.conf

[Install]
WantedBy=multi-user.target